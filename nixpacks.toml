[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "glib", "gobject-introspection", "cairo", "pango"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
